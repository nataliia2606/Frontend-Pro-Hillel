let firstLine = prompt("Enter the first line");
let secondLine = prompt("Enter the second line");
let thirdLine = prompt("Enter the third line");
console.log(`${firstLine} ${secondLine} ${thirdLine}`);
